-- unique candies
razor_apple = {
  name = "razor apple",
  sprite = 20,
  hp = 100,
  attack = 100,
  defense = 100,
  element = elements.grass,
  attacks = {
    punch,
    kick,
    rot_teeth,
    caramelize
  }
}

atomic_bomb_ball = {
  name = "atomic bomb ball",
  sprite = nil,
  hp = 150,
  attack = 100,
  defense = 100,
  element = elements.fire,
  attacks = {
    roll_over,
    hot_under_the_collar,
    fire_puke,
    split_atom
  }
}